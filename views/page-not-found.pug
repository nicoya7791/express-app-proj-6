extends layout.pug 

block content 
  nav.nav
    a(href='/')
      span &larr;
      |  Back
    //- show 404 and error friendly message when a page is not found.
    h1 Erro 404- page not found!
    h2 #{err.message}
    
  nav.nav
    a(href='/')
      span &larr;
      |  Back
